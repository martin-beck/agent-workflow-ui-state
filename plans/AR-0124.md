# AR-0124: SSH relay release and compatibility synchronization

Publish the endpoint candidate and tunnel contracts, update operator and
Android documentation, and synchronize the Agent Workflow compatibility lock.
Require merged product PRs, exact release tags, state reconciliation, and
live doctor checks before the umbrella pin changes.
