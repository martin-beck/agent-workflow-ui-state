# AR-0111: Android credential and lifecycle hardening

Harden device-key and session-credential storage, rotation, revocation,
replacement, logout, clock/expiry handling, and service restart recovery.
Qualify TLS, certificate failure, and offline reconnect behavior.

Acceptance: credentials never appear in QR payloads or logs; revoked devices
cannot poll or submit; replacement invalidates the previous registration.
