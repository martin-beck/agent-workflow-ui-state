# AR-0116: Android development release and compatibility evidence

Produce a reproducible unsigned debug APK with SBOM/build metadata, minimum
SDK/device compatibility documentation, checksums, and a release evidence
bundle linked to the exact product commit.

Acceptance: artifacts are downloadable from CI, checksums verify, and no
signing key or production credential is required.
