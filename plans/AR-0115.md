# AR-0115: Remote/network Android qualification

Exercise the phone service over localhost, LAN, SSH-forwarded HTTPS, and an
offline/reconnect transition. Verify endpoint identity, QR expiry, service
restart, and correct project-state event persistence.

Acceptance: CI network fixtures prove that a remote phone receives exactly one
authoritative batch and writes events to the matching remote state revision.
